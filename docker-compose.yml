services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      PLO_DATA_DIR: /app/data
      PLO_DEMO_MODE: "0"
    volumes:
      - plo_data:/app/data
    restart: unless-stopped
volumes:
  plo_data:
